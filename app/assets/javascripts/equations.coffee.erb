tinyMCE.init {
  selector: '.eq_editor',
  toolbar: 'undo redo | bold italic | image | link  | equationeditor',
  plugins: 'image, link, autoresize, equationeditor',
  content_css: "<%= asset_url 'assets/application.css' %>",
}
